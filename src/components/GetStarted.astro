<section class="get-started">
    <h2>Klar til din første forbrugspause?</h2>
  
    <p>
      Log ind og se dine point - eller opret en konto og kom i gang med at optjene
      forbrugspause-fordele med Dankort.
    </p>
  
    <a href="#" class="get-started-btn">Log ind eller opret konto</a>
  </section>

  <style>

.get-started {
  max-width: 430px;
  margin: 20px auto;
  padding: 28px 20px;
  background: var(--green);
  border-radius: 14px;
  text-align: center;
}

.get-started h2 {
  margin: 0 0 10px;
  font-size: var(--h2);
}

.get-started p {
  margin: 0 0 18px;
  line-height: 1.45;
  font-size: var(--p);
}

.get-started-btn {
  display: inline-block;
  background: var(--white);
  color: var(--black);
  text-decoration: none;
  font-size: var(--p);

  padding: 10px 16px;
  border-radius: 10px;
  border: 1px solid rgba(0, 0, 0, 0.25);
}
  </style>