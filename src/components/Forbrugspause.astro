<section class="hero">
  <h1 class="hero-title">
    <span class="green-highlight">Forbrugspause</span> med Dankort
  </h1>

  <p class="hero-subtitle">Tag en pause og optjen point</p>

  <div class="hero-images">
    <img src="/images/blad-logo.webp" alt="Grønt ikon" class="hero-icon" />

    <img src="/images/phone-app.webp" alt="Skærmbillede af Forbrugspause i appen" class="hero-phone" />
  </div>

  <p class="hero-text">Hent Dankort appen, hvor du optjener point, hver gang du tager en forbrugspause og bliver belønnet.</p>

  <a href="/forbrugspause">
    <button class="btn">Se hvordan</button>
  </a>
</section>

<style>
  .hero {
    background: var(--grey);
    border-radius: 10px;
    padding: 20px;
    text-align: center;
    max-width: 430px;
    margin: 20px auto;
    margin-top: 50px;

    display: grid;
    grid-template-columns: 1fr;
  }

  .hero-title {
    margin: 0 0 6px;
    font-size: var(--h1);
    font-weight: 700;
    line-height: 1.15;
  }

  .hero-subtitle {
    margin: 0 0 14px;
    font-size: 14px;
  }

  .hero-icon,
  .hero-phone {
    display: block;
    height: auto;
    max-width: none;
  }

  .hero-images {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    margin: 10px 0 14px;
  }

  .hero-icon {
    width: 70px;
  }

  .hero-phone {
    width: 140px;
  }

  .hero-text,
  .bundtekst {
    margin: 0;
    font-size: var(--p);
    line-height: 1.45;
  }

  .bundtekst {
    margin-top: -300px;
  }

  .green-highlight {
    background: var(--green);
    padding: 0 6px;
    border-radius: 4px;
  }

  .btn {
    padding: 8px 20px;
    background-color: var(--white);
    color: var(--black);
    border: 0.5px solid var(--black);
    border-radius: 10px;
    font-size: var(--btn);
    cursor: pointer;
  }

  .btn:hover {
    background-color: var(--green);
  }

  @media (min-width: 700px) {
    .hero {
      max-width: 1000px;
      padding: 48px;
      text-align: left;
      margin-top: 100px;

      grid-template-columns: 1.15fr 0.85fr;
      column-gap: 56px;
      /* align-items: center; */
      align-items: start;
      border-radius: 20px;
    }

    .hero-subtitle {
      font-size: 35px;
      margin-bottom: 18px;
      margin-top: -130px;
    }

    .hero-text {
      max-width: 60ch;
      margin-top: -220px;
    }

    .hero-images {
      grid-column: 2;
      grid-row: 1 / span 3;
      justify-content: flex-end;
      margin: 0;
      gap: 28px;
    }

    .hero-icon {
      width: clamp(90px, 10vw, 130px);
    }

    .hero-phone {
      width: clamp(220px, 22vw, 320px);
    }

    .btn {
      padding: 10px 80px;
      background-color: var(--white);
      color: var(--black);
      border: 1px solid var(--black);
      border-radius: 10px;
      font-size: var(--btn);
      cursor: pointer;
    }

    .btn:hover {
      background-color: var(--green);
    }

    .hero-title,
    .hero-subtitle,
    .hero-text,
    .bundtekst,
    .hero a {
      grid-column: 1;
    }

    .hero a {
      margin-top: 24px;
    }
  }
</style>
