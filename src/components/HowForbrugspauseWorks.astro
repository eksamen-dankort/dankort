<section class="how">
    <h2>Sådan fungerer din forbrugspause</h2>

  <div class="how-card">

    <div class="how-step">
        <span class="how-number">1</span>
      
        <div class="how-textline">
          <p class="how-text">Hent appen lige her</p>
          <img src="/images/arrow-right.svg" alt="" class="how-arrow" />
        </div>
      
        <a href="#" class="how-btn">Hent app</a>
      </div>

    <div class="how-step">
      <span class="how-number">2</span>
      <p class="how-text">Vælg de kategorier, du ønsker at holde pause fra</p>
  
      <img
        src="/images/categories-icon.svg"
        alt="" class="how-icon"
      />
    </div>

    <div class="how-step">
        <span class="how-number">3</span>
        <p class="how-text">Optjen point, modtag fede fordele og gør Danmark lidt grønnere</p>
    
        <img
          src="/images/reward-icon.svg"
          alt="" class="how-icon"
        />
      </div>
    </div>
  </section>





  <style>
h2{
    font-size: var(--h2);
    background: var(--grey);
  border-radius: 14px;
  padding: 20px;
}
.how {
    max-width: 430px;
    margin: 20px auto;
    padding: 0 20px;
  }
  
  .how-card {
    border-radius: 14px;
    padding: 20px;
  }
  
  .how-step {
    display: grid;
    grid-template-columns: 26px 1fr auto;
    gap: 20px;
    align-items: center;
    padding: 10px 0;
  }

  .how-textline {
  display: flex;
  align-items: center;
  gap: 20px;
}

.how-arrow {
  width: 50px;
  height: 50px;
  display: block;
}
  
  
  .how-number {
    font-weight: 700;
    font-size: 50px;
    color: var(--green);
    line-height: 1;
  }
  
  .how-text {
    margin: 0;
    font-size: var(--p);
  }
  
  .how-btn {
  background: var(--green);
  border: 0.5px solid var(--black);
  color: var(--black);
  border-radius: 8px;
  padding: 5px 40px;
  text-decoration: none;
  font-weight: var(--p);
  font-size: var(--p);
  white-space: nowrap;
}
  
  .how-icon {
    width: 60px;
    height: 60px;
    display: block;
  }


  /* -------- DESKTOP -------- */
@media (min-width: 700px) {
  .how {
    max-width: 1100px;
    padding: 0;        /* ingen ekstra indryk */
    margin: 24px auto 40px;
  }

  .how h2,
  .how-card {
    padding: 40px 48px; /* mere “luft” på desktop */
  }

  .how h2 {
    margin-bottom: 18px;
  }

  .how-step {
    grid-template-columns: 64px 1fr auto; /* mere plads til tal */
    gap: 28px;
    padding: 18px 0;
  }

  .how-number {
    font-size: 72px;
  }

  .how-icon {
    width: 84px;
    height: 84px;
  }

  .how-arrow {
    width: 64px;
    height: 64px;
  }
}
  </style>
