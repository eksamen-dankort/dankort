<section class="section">
  <div>
    <h1>Dankort.</h1>
    <p class="sub">Billigt. Sikkert. Og meget mere end det.</p>
    <p>Med Dankort betaler du, som du plejer – men vi arbejder samtidig på at gøre hverdagen lidt grønnere. Her kan du se, hvordan din betaling kan blive til både forbrugspauser, point og støtte til naturen.</p>
  </div>

  <img src="/public/images/blad-logo.webp" alt="greenicon" class="icon-large" />
</section>

<style>
  .section {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 16px;
    padding: 24px;
    background: var(--grey);
    border-radius: 10px;
    margin-bottom: 50px;
    margin-top: 50px;
    margin: 20px;
  }

  .section > div:first-child {
    max-width: 75%;
  }

  .icon-large {
    width: 60px;
    height: auto;
  }

  @media (min-width: 700px) {
    .section {
      background: var(--grey);
      padding: 60px;
      border-radius: 20px;
      margin-bottom: 50px;
      margin-top: 80px;
      display: flex;
      justify-content: space-between;
      margin-left: 170px;
      gap: 40px;
      max-width: 1000px;
    }

    .section h1 {
      font-size: var(--h1);
      margin: 0 0 20px 0;
    }

    .sub {
      font-size: 35px;
    }

    p {
      font-size: var(--p);
      line-height: 1.5;
      max-width: 650px;
    }

    .icon-large {
      width: 150px;
      height: 150px;
    }
  }
</style>
